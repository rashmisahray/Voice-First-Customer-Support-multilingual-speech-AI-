version: '3.8'

services:
  vani:
    build: .
    container_name: vani_support_agent
    ports:
      - "8000:8000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - ./data:/app/data
    restart: always
